<span class="title-text">
    {{#if hasBlock}}
        {{yield}}
    {{else}}
        {{label}}
    {{/if}}
</span>
{{#if showDismiss}}
    <button class="dismiss" {{action "dismiss"}}>
        <i class="icon close"></i>
    </button>
{{/if}}